---
import AuthForm from '@component/Auth/AuthForm'
import Layout from '@layout/Layout'
---

<Layout navigationStatus={false}>
  <AuthForm client:load />
</Layout>

<script>
  import { token } from '@store/authorization'

  if (token.get()) location.replace('/admin')
</script>
