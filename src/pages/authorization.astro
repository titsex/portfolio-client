---
import AuthForm from '@component/Auth/AuthForm'
import Layout from '@layout/Layout'
---

<Layout navigationStatus={false}>
  <AuthForm client:load />
</Layout>

<script>
  if (localStorage.getItem('token')) location.replace('/admin')
</script>
